你是一个资深软件工程师、AI工程师和技术写作者，擅长从源码出发拆解复杂项目。
我会给你一个本地项目路径，请你从代码出发（而不是仅看README）完整解析该项目，并生成一份可以让初学者快速上手 + 让进阶工程师深入理解实现细节的 Markdown 文档。
⚠️ 核心要求（必须遵守）：
1. 所有分析必须基于真实代码结构和调用关系
2. 不允许泛泛总结，必须结合“具体文件 / 类 / 函数”
3. 遇到关键逻辑，必须解释：
  - 做了什么
  - 为什么这么做
  - 有没有替代方案
4. 必须明确标出：
  - ⭐核心路径代码
  - ⚠️易错点 / 隐含逻辑
  - 💡设计亮点 / 技巧
🧭 一、项目快速理解（让人5分钟建立全局认知）
- 项目一句话目标（解决什么问题）
- 项目类型（模型 / 应用 / Agent / RAG / 工具 / 混合）
- 核心功能列表
- 一个典型使用流程（用户视角）
👉 输出一个“用户使用流程”：
例如：
用户输入 → 系统处理 → 模型/逻辑 → 输出结果
🏗 二、项目整体架构（必须结构化）
2.1 项目目录结构（必须解析）
逐层展开目录，并说明：
- 每个目录的职责
- 哪些是核心模块（标⭐）
2.2 模块关系图（文字流程图）
说明：
- 模块之间如何调用
- 数据如何流动
⚙️ 三、环境配置 & 最小运行路径（必须可复现）
- 依赖环境（Python / 框架 / 外部服务）
- 安装步骤（命令级别）
- 如何运行最小Demo（逐步说明）
- 程序入口文件是哪一个（必须定位到具体文件）
🔍 四、【核心】代码执行主路径拆解（必须按调用链分析）
⚠️ 本部分是最重要的，必须严格按“执行顺序”分析代码：
4.1 程序入口
- 找到 main / cli / app 启动文件
- 分析执行流程（逐步）
4.2 主调用链（必须画出来）
例如：
main.py → trainer.py → model.py → utils.py
👉 对每一步说明：
- 调用了哪个函数
- 输入是什么
- 输出是什么
🧠 五、核心模块源码精读（必须深入函数级别）
⚠️ 要求：不是“介绍模块”，而是“带着读代码”
对每个核心模块，按以下格式分析：
模块：xxx.py（路径）
1️⃣ 模块作用
这个模块解决什么问题？
2️⃣ 核心类/函数
列出最重要的类/函数
3️⃣ 关键代码解析（必须细）
对关键函数做“半逐行解析”（但不要机械逐行翻译）：
示例结构：
- Step1：加载输入
- Step2：处理数据（解释算法/逻辑）
- Step3：调用下游模块
👉 每一步必须说明：
- 输入数据结构
- 中间变量变化
- 为什么这么写
4️⃣ 数据流变化（非常重要）
说明：
数据在这个模块中：
输入 → 处理 → 输出
🔄 六、数据流全链路解析（必须串起来）
⚠️ 这是理解项目的关键
从“真实运行流程”角度描述：
1. 输入数据从哪里来（文件 / API / 用户）
2. 如何被加载（Dataset / Parser / Request）
3. 如何被处理（预处理 / embedding / feature）
4. 如何进入核心逻辑（模型 / 规则 / agent）
5. 如何生成输出
6. 输出如何返回（API / 文件 / UI）
👉 用“流水线方式”描述清楚
🧪 七、数据结构与输入输出示例（必须具体）
- 输入数据格式（JSON / tensor / text）
- 输出数据格式
- 举一个完整真实样例：
输入 → 中间过程 → 输出
🤖 八、（如适用）模型 / 算法解析
如果项目涉及模型：
- 模型结构（模块划分）
- forward流程（必须讲清数据怎么走）
- loss / 推理逻辑
- 与标准实现的区别
如果是AI应用（如RAG / Agent）：
- prompt构造逻辑
- tool调用机制
- memory / 检索机制
- orchestration流程
📊 九、评估 / 结果机制（如存在）
- 如何评估效果
- 指标计算方式
- 评估代码在哪
🧩 十、关键设计与核心贡献（必须提炼）
⚠️ 不是复述代码，而是总结：
- 这个项目最核心的3-5个设计点
- 与常见实现的区别
- 为什么这样设计更好
⚠️ 十一、潜在问题与改进建议（必须批判性分析）
- 代码结构问题
- 可扩展性问题
- 性能问题
- 可以如何优化
🚀 十二、如何二次开发（实用）
- 想加新功能 → 改哪里
- 想替换模型 → 改哪里
- 想换数据 → 改哪里
📌 输出要求（必须遵守）
- 使用 Markdown
- 结构清晰（标题分层）
- 多用列表 / 流程 / 小节
- 所有关键点尽量引用“具体文件路径 + 函数名”
- 避免空话（如“这是一个很重要的模块”）
- 重点地方必须解释“为什么”
现在开始解析这个项目路径：